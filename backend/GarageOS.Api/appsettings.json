{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    },
    "WriteTo": [
      { "Name": "Console" }
    ],
    "Enrich": [ "FromLogContext" ]
  },
  "AllowedHosts": "*",
  "Demo": {
    "Message": "PIT961 backend scaffold is alive (WP-2)."
  },
  "Jwt": {
    "_comment": "Never committed with a real value -- see JwtOptions.cs remarks. Production: environment variable / host secret store only.",
    "Issuer": "",
    "Audience": "",
    "SigningKey": ""
  },
  "PasswordReset": {
    "ResetLinkBaseUrl": "https://app.garageos.example/reset-password"
  },
  "ConnectionStrings": {
    "_comment": "No real connection strings/credentials are committed. Local dev overrides via dotnet user-secrets or a gitignored appsettings.Local.json; production via environment variables / host secret store. See README.md.",
    "GarageOsDb": "",
    "PlatformDb": ""
  },
  "Branding": {
    "_comment": "Product brand is undecided (see DECISIONS.md #6). These are the ONLY branding values consumed at runtime -- this placeholder is intentionally generic and must never be read as an implied final brand decision. Override per environment before any real customer traffic.",
    "ProductDisplayName": "Garage Management Platform",
    "EmailFromName": "Garage Management Platform",
    "LogoUrl": "",
    "SupportEmail": ""
  }
}
