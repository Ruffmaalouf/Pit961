{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug"
    }
  },
  "Demo": {
    "Message": "PIT961 backend scaffold is alive (WP-2, Development environment)."
  },
  "ConnectionStrings": {
    "_comment": "Safe, credential-free local default (trust-auth local Postgres, no real secret). Real non-local values still go through user-secrets/appsettings.Local.json per README.md.",
    "GarageOsDb": "Host=localhost;Port=5432;Database=pit961_dev;Username=postgres;Password=",
    "PlatformDb": "Host=localhost;Port=5432;Database=pit961_dev;Username=postgres;Password="
  }
}
