# IMPLEMENTATION_MAP.md

Live map of every Phase 1 work package to its implementation location. Updated
as each WP starts/lands. See `13_phase1_execution_plan.md` for full scope and
acceptance criteria per WP, and `PROGRESS.md` for narrative status.

| WP | Title | Specialist | Status | Location / Notes |
|----|-------|------------|--------|-------------------|
| WP-1 | Repo & Environment Bootstrap | Technical Architect | **DONE** | Repo root: `.git/`, `.gitignore` (hardened), `README.md`, `frontend/`, `backend/`. Initial commit `ba3ee96`. |
| WP-10 | Engineering Tracking Docs | Dispatcher / Technical Architect | **DONE** | This file + `PROGRESS.md`, `TEST_STATUS.md`, `KNOWN_ISSUES.md` (repo root). |
| WP-2 | Backend Solution Scaffold | Backend Engineer | **DONE** | `backend/` — ASP.NET Core 8 modular monolith (`GarageOS.Api`/`GarageOS.Application`/`GarageOS.Domain`/`GarageOS.Infrastructure`) + `GarageOS.Tests.Unit`/`GarageOS.Tests.Integration`. Commit `a6bb20a`. QA: PASS (QA Automation Engineer). Security: PASS (Security Reviewer). |
| WP-3 | Schema / Tenant Isolation | Database Engineer + Backend Engineer | **DONE** | `backend/GarageOS.Domain` (16 entities), `backend/GarageOS.Application` (`ICurrentTenant`, `TenantGuard`), `backend/GarageOS.Infrastructure` (`AppDbContext`/`PlatformDbContext`, 16 entity configs, both migration sets, dev seeder). Commits `61b4bf9`, `067e4d1`, `0fc3201`, `fb975b9`. Database Engineer review: APPROVE (follow-ups remediated). QA Automation: PASS. Security: PASS. QA Lead: **ACCEPT WITH TRACKED FOLLOW-UPS** (no BLOCKER/CRITICAL). |
| WP-3B | Account/Garage Provisioning Service | Backend Engineer + Database Engineer | **DONE** | `backend/GarageOS.Application/Abstractions/IAccountProvisioningService.cs`, `.../Accounts/GarageProvisioningDetails.cs`, `.../Common/Account{AlreadyHasGarage,NotFound}Exception.cs`; `backend/GarageOS.Infrastructure/Data/Provisioning/AccountProvisioningService.cs` (locks the parent `accounts` row via `FOR UPDATE`, not `garages` -- phantom-row fix); migration `MakeGaragesAccountActiveIndexUnique` (partial unique index `garages_account_active_idx`, with documented pre-flight duplicate-check); `DevelopmentSeeder.cs`/`Program.cs` updated to the service exclusively; bypass-protection source-scan test (`GarageInsertBoundaryTests`); 8 behavioral + 2 concurrency integration tests. Zero HTTP endpoint added, per Owner instruction. Commit `b289315`. Database Engineer review: **ACCEPT** (brief-stage required change confirmed incorporated). QA Automation: **PASS WITH FINDINGS** (0 BLOCKER/CRITICAL; 2 MEDIUM follow-ups tracked as KI-8/KI-9). |
| WP-4 | Authentication / JWT / Platform Admin claim | Backend Engineer | **DONE** | JWT (HS256) access tokens + rotating/revocable/reuse-detected refresh tokens; `backend/GarageOS.Application/{Auth/AuthService.cs,Configuration/{JwtOptions,PasswordResetOptions}.cs,Common/OpaqueTokenGenerator.cs,Abstractions/{IPasswordHasher,ITokenService,IUserAuthLookupRepository,IRefreshTokenRepository,IPasswordResetTokenRepository,IEmailService,IPasswordResetRequestQueue}.cs}`; `backend/GarageOS.Infrastructure/{Security/{TokenService,PasswordHasherService,Authorization/{GarageTenantRequirement,PlatformAdminRequirement}}.cs,Data/Auth/*.cs,BackgroundJobs/*.cs,Email/NoOpEmailService.cs}`; `backend/GarageOS.Api/{Controllers/AuthController.cs,Contracts/AuthContracts.cs,Program.cs (full wiring),appsettings.Testing.json (test-only committed signing key)}`; new entity `PasswordResetToken`; migration `AddUserLockoutColumns` (lockout columns + `password_reset_tokens` table, combined by EF's diff); `TestAuthHandler` retired, replaced by `TestJwtTokenFactory` (mints real tokens via the production `ITokenService`). `"GarageTenant"`/`"PlatformAdminOnly"` authorization policies; zero live platform-admin HTTP route (per Owner constraint), proven by `PlatformAdminRouteInventoryTests`. Rate limiting (`Microsoft.AspNetCore.RateLimiting`, configurable via `RateLimiting:*`, production defaults 5/20/3/5 per brief §15) and account lockout (5 failures / 15 min) both server-side enforced. KI-3 (demo config endpoint) and KI-4 (ProblemDetails exception-field negative test) closed opportunistically. 46 new/modified tests, all passing against real PostgreSQL 15.19 — 121/121 `GarageOS.Tests.Integration` + 5/5 `GarageOS.Tests.Unit`, zero regressions. Security Reviewer: **BLOCKED, then formally re-invoked for a targeted remediation re-review — verdict PASS — HIGH CLOSED** (one HIGH finding — refresh-token rotation race allowing concurrent reuse to bypass reuse-detection — fixed via an atomic conditional-UPDATE claim in `TryClaimForRotationAsync`, regression-tested by `Refresh_ConcurrentPresentationOfSameToken_ExactlyOneWinsAndReuseDetectionStillFires`; the fix was independently re-reviewed by the Security Reviewer agent against a fresh code snapshot -- not cleared on automated-test passage alone -- confirming the atomic claim closes the race, both row-count branches are handled correctly with no path to JWT issuance on a failed claim, no alternate rotation path bypasses the claim, and reuse-detection fan-out is preserved; one new non-blocking LOW finding logged as KI-15; KI-7 confirmed closed at the code level). QA Automation: **PASS WITH FINDINGS** (0 BLOCKER/CRITICAL; 2 MEDIUM + 3 LOW tracked as KI-10..KI-14; password-length boundary gap closed immediately). Technical Architect: **ACCEPT** (all 6 brief-stage required changes confirmed incorporated; 2 non-blocking notes, 1 closed immediately). Commit `65fe0a6`. |
| WP-5 | Authorization Policies (discount 15%, $500 threshold) | Backend Engineer + Technical Architect review | **DONE** | Resource-based ASP.NET Core policies (never bare `[Authorize(Policy=...)]` -- both handlers' doc comments warn against it): `backend/GarageOS.Infrastructure/Security/Authorization/{DiscountLimitRequirement,EstimateApprovalThresholdRequirement}.cs` (`DiscountLimitHandler`: manager cap 15.00%, roles permitted {owner,manager}, tenant_mismatch/role_not_permitted/exceeds_manager_cap; `EstimateApprovalThresholdHandler`: $500.00 threshold, role-blind past tenant check, tenant_mismatch/requires_owner_approval -- the latter is a deliberate ROUTING signal, not a rejection, distinguished by failure-reason string). `backend/GarageOS.Application/{Abstractions/{IBusinessRuleAuthorizer,IEstimateMutationRepository}.cs,Estimates/{EstimateDiscountService,EstimateApprovalService}.cs}` (framework-free Application layer, bridged to real `IAuthorizationService` via `backend/GarageOS.Infrastructure/Security/Authorization/AspNetBusinessRuleAuthorizer.cs`, mirroring WP-3's `ICurrentTenant`/`HttpContextCurrentTenant` split). `backend/GarageOS.Infrastructure/Data/Estimates/EstimateMutationRepository.cs` is the SOLE authoritative writer of `Estimate.DiscountAmount/Total/Status` (fresh `AsNoTracking` read + fresh re-fetch-and-set-properties write, never a tracked-instance flush). Migration `AddPendingOwnerApprovalEstimateStatus` widens the `ck_estimates_status` CHECK constraint. Zero new HTTP endpoints (brief explicitly scoped tests to handler/application-service/authorization-infrastructure level, per Owner's "no fake Phase 3 endpoints" instruction). Bypass protection: `backend/GarageOS.Tests/GarageOS.Tests.Unit/Architecture/{EstimateMutationBoundaryTests,AuthorizationAttributeMisuseTests,SourceScanUtilities}.cs` -- automated (not one-off grep) source-scan tests proving only `EstimateMutationRepository` may mutate Estimate rows and only the two pre-approved WP-4 literal policy names may be attached via `[Authorize(Policy=...)]`. Went through FOUR rounds of adversarial hardening this session (see `KNOWN_ISSUES.md` KI-16/KI-17 for the full history): round 1 (QA Automation) found/fixed 3 bypasses (non-generic `DbContext.Update`/`Attach` evading a DbSet-rooted regex, a long `.Where()` chain evading a fixed character window, named-constant indirection evading a policy-name block-list); round 2 (QA Automation) found/fixed 2 more (a `;` inside a string literal defeating naive statement-splitting, C# attribute-stacking defeating a line-start heuristic) via a new shared `SourceScanUtilities.MaskLiteralsAndComments` helper; round 3 (QA Lead's own gate review) found/fixed 1 more (an interpolated string's `{...}` hole content being blanked instead of treated as live code, hiding a real `.Update(` call) and fully closed KI-16 (widened the Update/Attach patterns to be unqualified, since grep confirmed zero legitimate call sites anywhere in the solution); round 4 (Security Reviewer's own gate review) found/fixed 1 more (a nested string literal declared inside a hole was mis-classified as plain instead of checking its own `@`/`$` prefix, hiding a call nested two levels deep and/or corrupting downstream masking) -- independently re-verified past the reported shapes with 3-level nesting and mixed prefix ordering. 25 new `GarageOS.Tests.Unit` test cases (6 `DiscountLimitHandlerTests` + 4 `EstimateApprovalThresholdHandlerTests` [Theory-expanded to 5 runs] + 5 `EstimateDiscountServiceTests` + 3 `EstimateApprovalServiceTests` + 2 `EstimateMutationBoundaryTests` + 1 `AuthorizationAttributeMisuseTests`), all passing -- **30/30 `GarageOS.Tests.Unit` total.** Zero new integration tests (out of WP-5's own scope); **121/121 `GarageOS.Tests.Integration`** re-confirmed with no regressions (one pre-existing, unrelated environmental flake, `JwtValidationTests.Me_TamperedSignature_ReturnsUnauthorized`, reproduces intermittently under full-batch load but passes 100% in isolation -- not a WP-5 change). QA Lead: **BLOCKED** (round-3 interpolation-hole finding) **-> PASS** after fix, independently re-verified via injected/deleted PoC files. Security Reviewer: **BLOCKED** (round-4 nested-literal-in-hole finding) **-> PASS** after fix, independently re-verified via injected/deleted PoC files including novel payloads beyond the ones originally reported. Commit `3b7e535`. |
| WP-6 | Email (IEmailService / Resend) | Integration Engineer (email-provider specialist, owns `ResendEmailService`/Resend SDK boundary) + Backend Engineer (collaborates on `AuthService`/password-reset wiring) + Company Dispatcher (device execution) | **DONE** | `backend/GarageOS.Application/Abstractions/IEmailService.cs` grew a `SendTransactionalAsync` method (approved Phase 1 infrastructure per 11_engineering_handoff.md §11A/DECISIONS.md, no current caller -- not scope creep, independently confirmed by both QA Lead and Security Reviewer). `backend/GarageOS.Infrastructure/Email/ResendEmailService.cs` (new) -- the ONLY class permitted to reference the Resend API (Decision #8): a hand-rolled typed-HttpClient POST against `https://api.resend.com/emails`, deliberately not a third-party SDK package (none referenced anywhere in the repo), which makes the SDK-isolation acceptance criterion mechanically true (no SDK type exists to leak). Registered via `AddHttpClient<IEmailService, ResendEmailService>` in `Program.cs`, replacing the WP-4 `NoOpEmailService` registration (kept, unregistered, as a documented fallback/ swap-proof -- also gained `SendTransactionalAsync`). New `backend/GarageOS.Application/Configuration/ResendOptions.cs` (ApiKey, FromAddress) -- ApiKey follows `JwtOptions`' secrets pattern exactly (`.Validate()` + `.ValidateOnStart()`, empty placeholder in appsettings.json/appsettings.Development.json, fixed obviously-fake test-only value in appsettings.Testing.json). `AuthService.ProcessForgotPasswordRequestAsync` unchanged except for which `IEmailService` implementation DI resolves -- all WP-4 anti-enumeration/rate-limiting/token-expiry/single-use behavior confirmed unweakened. `CapturingEmailService` (integration test double) updated for the grown interface; `ForgotPasswordTests` gained a template/subject assertion. Resend-SDK-isolation CI regression check: `scripts/ci/check-no-resend-outside-service.sh` (allow-listing only `ResendEmailService.cs`, unrestricted by file extension otherwise, matching WP-7's `check-no-legacy-brand.sh` pattern) wired into `.github/workflows/ci.yml` as a blocking step; negative-test proof run and independently re-run by both gates via actual script execution. 10 new tests, all passing -- 10 `GarageOS.Tests.Unit` (`Email/ResendEmailServiceTests` x6, `ResendOptionsBindingTests` x4) + 0 new integration test classes (one assertion added to an existing `ForgotPasswordTests` case) -- **180/180 total, zero regressions.** QA Lead: **PASS** (independently built/ran both suites and reproduced the CI check's negative-test proof itself; one MINOR documentation-parity note, fixed). Security Reviewer: **PASS** (traced the actual HTTP call/logging code directly; confirmed no SSRF/injection risk, hardcoded HTTPS, a 10s request timeout, and that `EnsureSuccessStatusCode()`'s default exception message cannot leak a Resend response body; one LOW/informational note -- the Authorization header was being set on the shared `HttpClient.DefaultRequestHeaders` rather than per-request, a latent race for a future long-lived caller though safe under today's call pattern -- fixed opportunistically by building the `HttpRequestMessage` explicitly and re-verified, 180/180 still passing). Committed as `f8606b1` (verified via `git log`). |
| WP-7 | Branding Configuration | Technical Architect (brief) + Company Dispatcher (device execution) + Backend Engineer (collaboration) | **DONE** | `backend/GarageOS.Application/Configuration/BrandingOptions.cs` (ProductDisplayName/EmailFromName/LogoUrl/SupportEmail, no `.Validate(...)` clause, matching `DemoOptions` precedent -- none of the four fields is secret/startup-fatal); `backend/GarageOS.Api/Program.cs` (`AddOptions<BrandingOptions>().BindConfiguration(...).ValidateOnStart()`, registered independently of the JWT bearer pipeline -- zero shared code path, confirmed by both QA Lead and Security Reviewer); `backend/GarageOS.Api/Contracts/ConfigContracts.cs` (`BrandingConfigResponse`, a closed hand-mapped DTO, never a direct serialization of `BrandingOptions`); `backend/GarageOS.Api/Controllers/ConfigController.cs` (`[AllowAnonymous] GET /api/config/branding`, sole constructor dependency `IOptions<BrandingOptions>`, reflection-proven structurally incapable of reaching `IConfiguration`/`JwtOptions`/connection strings by `ConfigControllerDependencySurfaceTests`); `Branding` sections added to `appsettings.json`/`appsettings.Development.json`/`appsettings.Testing.json` (placeholder values, e.g. `"Garage Management Platform"` -- final brand still undecided per DECISIONS.md #6). No new migration (pure configuration/options layer, no schema change). Placeholder-brand ("Rashid") CI regression check: `scripts/ci/check-no-legacy-brand.sh` (unrestricted `grep -rlni` over backend/frontend, excluding only node_modules/bin/obj/dist/.git/.review-snapshots -- matches the Owner-approved acceptance criterion in `13_phase1_execution_plan.md` verbatim) wired into `.github/workflows/ci.yml` as a blocking step; negative-test proof run and independently re-run by both QA Lead and Security Reviewer via actual script execution (fail-and-list, remove-and-pass) across .cs/.txt/.yaml/.svg/.resx. 19 new tests, all passing -- 7 `GarageOS.Tests.Unit` (`BrandingOptionsBindingTests` x4, `Architecture/ConfigControllerDependencySurfaceTests` x3) + 12 `GarageOS.Tests.Integration` (`BrandingConfigEndpointTests` x10, `BrandingConfigPropagationTests` x1, `BrandingJwtDecouplingTests` x1) -- **170/170 total, zero regressions.** `BrandingJwtDecouplingTests` is the key strict-separation proof: boots two hosts from the same compiled `Program` with identical `Jwt:*` config but different `Branding:ProductDisplayName`, logs in via the real `/api/v1/auth/login` path on both, and asserts the decoded issued tokens' iss/aud claims are identical and equal to the fixed expected values -- a runtime proof, not just a code-inspection claim. QA Lead: **PASS** (2 rounds -- round 1 found 2 CRITICALs: the CI check's file-extension allow-list was an unauthorized narrowing of the Owner-approved unrestricted scan, missing `.txt`/`.yaml`/`.svg`/`.resx`; and the CI step's comment cited a PROGRESS.md negative-test record that didn't yet exist -- both fixed [extension allow-list removed entirely; comment made self-contained and accurate] and independently re-verified by QA Lead executing the corrected script itself). Security Reviewer: **PASS** (independently re-executed the negative-test proof and traced the JWT-decoupling test logic rather than trusting QA's sign-off; one LOW/advisory note carried forward to WP-8, not blocking WP-7: validate `LogoUrl`/`SupportEmail` scheme/encoding before ever rendering them client-side, since no frontend exists yet to pose that risk today). Committed as `49af255` (verified via `git log`). |
| WP-8 | Frontend Tooling Scaffold | Frontend Engineer | **ACCEPTED (2026-09-02)** | `frontend/` — React 18 + TS + Vite + Tailwind + shadcn, with real WP-4 auth integration and WP-7 branding wiring. All three required specialist gates passed: Design Lead (PASS), QA Lead (PASS), Security Reviewer (PASS, two rounds — round 1 found and required a fix for CRITICAL/HIGH `npm audit` findings in `vitest`/`vite`, round 2 independently re-audited from a clean `node_modules`-free state and confirmed 0 CRITICAL/HIGH). See `PROGRESS.md` "WP-8: Frontend Scaffold" section for full narrative. |
| WP-9 | CI Pipeline | DevOps Engineer + Technical Architect | ACCEPTED (2026-09-02) | `.github/workflows/ci.yml`: `build-and-test` (backend build/migrations/tests vs real Postgres 15), `build-and-test-frontend` (frontend build/typecheck/Vitest), and `e2e-frontend` (WP-9 primary gap, closed 2026-09-02, commits `569408f`/`d050ec0`/`a9833fd`: its own Postgres 15 service, both DbContext migrations, real backend in `Development` (dev-seed fires) health-gated on `/health`, real Vite dev server health-gated, the real unchanged WP-8 Playwright suite, failure log capture, always-cleanup). Locally dry-run verified end-to-end on-device (4/4 Playwright passing). DevOps Engineer, Technical Architect, QA Lead, and Security Reviewer WP-9 gates all PASS against the real committed file (Security: 1 MEDIUM finding, resolved same day — see KI-19). **ACCEPTED**: real GitHub remote created (`https://github.com/Ruffmaalouf/Pit961`, Owner-provided PAT). Real happy-path CI run green (https://github.com/Ruffmaalouf/Pit961/actions/runs/33629806752). Real negative-gate proof completed on 3 temporary never-merged branches/PRs (failing test, Rashid, Resend), each failed at exactly the intended step, then cleaned up -- `main` confirmed clean. 3 consecutive clean full CI runs confirmed (runs `33629806752`, `33631966279`, `33632393075`). |

**Standing note:** no Docker/containerization anywhere in this repo (Owner decision, `DECISIONS.md` #10). Local dev: `dotnet run`/`dotnet watch`, `npm run dev`, PostgreSQL 15+ installed/reachable directly.
